# Target 31: Long Binder
# Longer binder (150 residues) — tests scaling, memory, attention window

target:
  file: "../structures/5o45.cif"
  chains:
    A:
      crop: ["1-116"]
      hotspots: [40, 99, 107]

binder_length: 150
